#include <stdio.h>

int main()
{
    /*
    the int type can storage
    integer numbers in a range
    that depends on the architecture
    */
    int numA = 5;
    int numB =-14524;


    /*
    the float type can storage
    floating point numbers in a range
    that depends on the architecture
    */
    float numC = 5.4f;
    float numD =-3.7f;

    /*
    the char type can storage
    ASCII characters*/
    char letterA = 'A';
    char letterB ='f';

    return 0;
}